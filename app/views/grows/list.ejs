<% include ../partials/header.ejs %>

<div class="viewport-container pb-5">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12 col-md-6 ml-md-auto mr-md-auto text-center">
                <div class="card mt-3">
                    <div class="card-body">
                        <h1 class="card-title">List All Grows</h1>

                        <p class="card-text">
                            There are currently <%= grows.length %> Grows.
                        </p>

                    </div><!-- .card-body -->
                    <% if(grows.length > 0){ %>
                        <ul class="list-group list-group-flush">
                            <% grows.forEach((grow) => { %>
                                <a href="/grows/<%= grow._id %>" class="list-group-item list-group-item-action">
                                    <%= grow.name %> - <%= grow.user.local.username %> - <%= grow.plants.length %> Plants
                                </a>
                            <% }); %>
                        </ul>
                    <% } %>                       
                </div><!-- .card -->
            </div><!-- .col -->
        </div><!-- .row -->    
    </div><!-- .container-fluid -->            
</div><!-- .viewport-container -->

<% include ../partials/footer.ejs %>